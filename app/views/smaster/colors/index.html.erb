<p id="notice"><%= notice %></p>

<h1>Colors</h1>

<table>
  <thead>
    <tr>
      <th>Colorname</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @colors.each do |color| %>
      <tr>
        <td><%= color.colorname %></td>
        <td><%= link_to 'Show', smaster_color_url%></td>
        <td><%= link_to 'Edit', edit_smaster_color_path(color) %></td>
        <td><%= link_to 'Destroy', smaster_color, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Color', new_smaster_color_path %>
