<%= form_with(model: type, local: true) do |form| %>
  <% if type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(type.errors.count, "error") %> prohibited this type from being saved:</h2>

      <ul>
      <% type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :suit_type %>
    <%= form.text_field :suit_type, id: :type_suit_type %>
  </div>

  <div class="field">
    <%= form.label :start_limit %>
    <%= form.number_field :start_limit, id: :type_start_limit %>
  </div>

  <div class="field">
    <%= form.label :end_limit %>
    <%= form.number_field :end_limit, id: :type_end_limit %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
